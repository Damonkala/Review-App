<!DOCTYPE html>
<html>
  <head>
    <base href="/">
    <meta charset="UTF-8">
    <title>scaffold</title>
    <link href="css/style.css" rel="stylesheet">
    <link rel="stylesheet" href="vendor/bootstrap/dist/css/bootstrap.min.css">

  </head>
  <body ng-app="scaffoldApp">
    <h1>Index</h1>
    <p>CHANGES</p>
    <div ng-controller="navCtrl">
      <span ng-if="isAuthenticated()">
        <button ui-sref="me" class="btn btn-default navbar-btn">profile</button>
        <button ui-sref="authorsList" class="btn btn-default navbar-btn">users</button>
        <button ng-click="logout()" class="btn btn-default navbar-btn">logout</button></span>
    </div>
    <div ui-view></div>

    <script src="assets/angular/angular.min.js"></script>
    <script src="assets/angular-ui-router/release/angular-ui-router.min.js"></script>
    <script src="/assets/satellizer/satellizer.min.js"></script>
    <script src="bundle.js"></script>
  </body>
</html>
